# Playwright MCP runtime: browser profile, console logs, screenshots
.playwright-mcp/

# Per-user Claude Code settings; should not be committed
.claude/

# Reference HTML snapshots used during development
sample_pages/

# Run feedback / historical notes; preserved locally but not part of the plugin
notes/

# Node (defensive — no package.json today, but the launcher uses npx)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
